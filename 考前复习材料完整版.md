# 软考系统架构设计师 — 考前复习材料完整版

> 基于考纲和历年真题，按知识点逐一整理，每节包含：
> ***背景*** | ***基础原理推导*** | ***公式总结*** | ***案例分析*** | ***难题解析***

---

## 目录

1. [计算机硬件基础](#1-计算机硬件基础)
2. [操作系统知识](#2-操作系统知识)
3. [数据库系统](#3-数据库系统)
4. [嵌入式技术](#4-嵌入式技术)
5. [计算机网络](#5-计算机网络)
    - 5.1 OSI 七层模型与 TCP/IP 协议族
    - 5.2 核心协议详解（ARP/RARP/ICMP/IGMP）
    - 5.3 传输层协议（TCP/UDP）
    - 5.4 应用层协议（FTP/HTTP/SMTP/POP3/DHCP/DNS）
    - 5.5 交换与路由技术
    - 5.6 传输介质与通信方式
    - 5.7 IP 地址与子网规划
    - 5.8 网络规划与结构化布线
    - 5.9 存储技术与 RAID
    - 5.10 其他网络技术（NAT/VLAN/VPN）
6. [系统配置与性能评价](#6-系统配置与性能评价)
7. [信息系统基础知识](#7-信息系统基础知识)
8. [系统安全](#8-系统安全)
9. [软件工程](#9-软件工程)
10. [面向对象技术](#10-面向对象技术)
11. [项目管理](#11-项目管理)
12. [系统架构设计](#12-系统架构设计)
13. [软件可靠性基础](#13-软件可靠性基础)
14. [软件架构的演化和维护](#14-软件架构的演化和维护)
15. [未来信息综合技术](#15-未来信息综合技术)
16. [补充-数学与经济管理](#16-补充-数学与经济管理)
17. [补充-知识产权与标准化](#17-补充-知识产权与标准化)

---

## 1. 计算机硬件基础

### 1.1 流水线技术

#### ***背景***

流水线技术产生于 20 世纪 60 年代，是为了解决 CPU 指令执行效率低下的问题而提出的。在早期计算机中，指令是顺序执行的：一条指令完全执行完毕后，下一条指令才能开始。这种方式导致 CPU 的各个功能单元（取指单元、译码单元、执行单元）在大部分时间处于空闲状态，资源利用率极低。

**应用场景**：
- 现代 CPU 的指令执行（超标量、超流水线）
- 工业生产流水线（汽车制造、电子产品组装）
- 数据处理管道（ETL、日志分析）
- 编译器的多阶段处理（词法分析→语法分析→代码生成）

**解决的核心问题**：通过将指令执行分解为多个独立阶段，使不同指令的不同阶段可以重叠执行，从而大幅提高系统吞吐量和资源利用率。

#### ***基础原理推导***

**核心逻辑拆解**：
1. **阶段划分**：将一条指令的执行过程分解为 k 个独立的子阶段（如取指、译码、执行、写回）
2. **并行执行**：当第 1 条指令完成第 1 阶段进入第 2 阶段时，第 2 条指令可以同时开始第 1 阶段
3. **时间重叠**：理想情况下，每个时钟周期可以完成一条指令

**推导过程**：
- 不使用流水线时，执行 n 条指令的总时间 = n × (t₁ + t₂ + ... + tₖ)
- 使用流水线时：
  - 第 1 条指令需要完整流过所有 k 个阶段：时间 = t₁ + t₂ + ... + tₖ
  - 后续 (n-1) 条指令每个周期完成一条：时间 = (n-1) × max(t₁, t₂, ..., tₖ)
- 总时间 = 建立时间 + 稳定时间

**扩展概念：超标量与超流水线**
- **超标量**：每个时钟周期发射多条指令，需要多个执行单元
- **超流水线**：将每个阶段进一步细分为更小的子阶段，提高主频
- **VLIW（超长指令字）**：编译器在编译时决定哪些指令并行执行

**流水线冒险（Pipeline Hazards）**：
- **结构冒险**：硬件资源冲突（如同一周期既要取指又要访存）
- **数据冒险**：指令之间存在数据依赖（RAW/WAR/WAW）
- **控制冒险**：分支指令导致流水线断流（分支预测技术解决）

#### ***公式总结***

| 指标 | 公式 | 说明 |
|------|------|------|
| **流水线周期** | Δt = max(t₁, t₂, ..., tₖ) | 各阶段中最长的执行时间（瓶颈） |
| **总执行时间** | T = Σtᵢ + (n-1) × Δt | n 为指令条数，Σtᵢ为单条指令总时间 |
| **吞吐率（TP）** | TP = n / T | 单位时间完成的指令数 |
| **最大吞吐率** | TPₘₐₓ = 1 / Δt | n→∞时的极限吞吐率 |
| **加速比（S）** | S = 顺序执行时间 / 流水线执行时间 | S = (n×Σtᵢ) / (Σtᵢ + (n-1)Δt) |
| **最大加速比** | Sₘₐₓ = k（各阶段时间相等时） | n→∞时的极限加速比 |
| **效率（E）** | E = 实际占用时空区 / 总时空区 | 理想情况下 E→1（n→∞时） |

**特殊情况**：当各阶段时间相等（均为 t）时，公式简化为：
- T = (k + n - 1) × t
- 最大加速比 Sₘₐₓ = k（n→∞时）

**单缓冲区 vs 双缓冲区公式**：
- 单缓冲区：T = (t₁ + t₂ + t₃) + max(t₁ + t₂, t₃) × (n-1)
- 双缓冲区：T = Σtᵢ + max(t₁, t₂, t₃) × (n-1)

#### ***案例分析***

**【2023 年真题】**
某流水线有 5 段，其中 1 段时间为 2ns，另外 4 段每段时间为 1ns。利用此流水线完成 100 个任务的吞吐率约为多少？

**解答**：
1. 流水线周期 Δt = max(2,1,1,1,1) = 2ns
2. 总执行时间 T = (2+1+1+1+1) + (100-1)×2 = 6 + 198 = 204ns
3. 吞吐率 TP = 100 / 204ns ≈ 0.49 × 10⁹ 个/秒 = 490 × 10⁶ 个/秒

**【2022 年真题】**
某浮点加法运算器分为 5 段，所需时间分别是 6ns、7ns、8ns、9ns 和 6ns。求其最大加速比。

**解答**：
1. 顺序执行时间 = (6+7+8+9+6)×n = 36n
2. 流水线周期 Δt = 9ns
3. 流水线执行时间 = 36 + 9(n-1) = 9n + 27
4. 加速比 S = 36n / (9n + 27)
5. 当 n→∞时，最大加速比 Sₘₐₓ = 36 / 9 = 4

**【真题：计算流水线吞吐率】**
某指令流水线由 5 段组成，各段执行时间分别为 1ns、3ns、2ns、2ns、1ns。连续输入 10 条指令，求该流水线的吞吐率。

**解答**：
Δt = max(1,3,2,2,1) = 3ns
Σtᵢ = 1+3+2+2+1 = 9ns
T = 9 + (10-1)×3 = 9 + 27 = 36ns
TP = 10 / 36ns ≈ 2.78 × 10⁸ 条/秒

#### ***难题解析***

**【拔高题·单双缓冲区】**
假设磁盘块与缓冲区大小相同，每个盘块读入缓冲区的时间为 15μs，由缓冲区送至用户区的时间是 5μs，在用户区内系统对每块数据的处理时间为 1μs。若用户需要将大小为 10 个磁盘块的文件逐块处理，求采用单缓冲区和双缓冲区分别需要花费的时间。

**解析**：

**单缓冲区情况**：
- 这是两段流水线：[读入缓冲区 + 送至用户区] 与 [用户处理]
- 前两段必须合并（缓冲区被占用时无法读入下一块）
- 合并后每块时间 = 15 + 5 = 20μs
- 总时间 = (20 + 1) + 20×(10-1) = 21 + 180 = 201μs

**双缓冲区情况**：
- 这是标准三段流水线：读入→传送→处理
- 各阶段可完全重叠
- 流水线周期 = max(15,5,1) = 15μs
- 总时间 = (15+5+1) + 15×(10-1) = 21 + 135 = 156μs

**易错点**：单缓冲区时不能直接套用三段流水线公式，因为前两个阶段共享同一个缓冲区资源，必须串行执行。

**【拔高题·流水线加速比极限】**
某流水线有 4 段，各段执行时间分别为 2t、3t、t、2t。若连续处理 100 个任务，该流水线的加速比是多少？最大可能加速比是多少？

**解析**：
Σtᵢ = 2t+3t+t+2t = 8t
Δt = max(2t,3t,t,2t) = 3t
T = 8t + (100-1)×3t = 8t + 297t = 305t
顺序执行时间 = 100 × 8t = 800t
加速比 S = 800t / 305t ≈ 2.62
最大加速比 Sₘₐₓ = 8t / 3t ≈ 2.67（当 n→∞时）

---

### 1.2 存储系统与 Cache

#### ***背景***

存储系统的分级结构源于"容量-速度-成本"的三角矛盾：CPU 寄存器速度极快但容量极小，主存速度中等容量中等，磁盘容量极大但速度极慢。为了在成本可接受的前提下获得尽可能高的性能，计算机系统采用了分级存储体系。

Cache（高速缓存）技术于 1965 年在 IBM System/360 Model 85 中首次实现，其理论基础是程序的局部性原理。

**应用场景**：
- CPU 与主存之间的 L1/L2/L3 缓存
- 数据库查询缓存
- Web 应用的 Redis/Memcached 缓存
- CDN 内容分发网络

**解决的核心问题**：以较小的成本代价，使存储系统的平均访问速度接近最快的存储介质，同时容量接近最大的存储介质。

#### ***基础原理推导***

**局部性原理**：
1. **时间局部性**：如果一个数据项正在被访问，那么在近期它很可能会被再次访问（循环、常用变量）
2. **空间局部性**：如果一个数据项正在被访问，那么它附近的数据项也很可能很快被访问（数组遍历、顺序执行）

**Cache 工作机制**：
1. CPU 发出内存访问请求
2. 检查该地址是否在 Cache 中（命中判断）
3. 命中：直接从 Cache 读取（快速）
4. 未命中：从主存读取，同时将该数据块载入 Cache

**地址映射方式**：
- **直接映射**：主存块 i → Cache 块 i mod C（C 为 Cache 总块数）。实现简单，但冲突率高
- **全相联映射**：主存任意块 → Cache 任意块。冲突率最低，但硬件复杂度高
- **组相联映射**：主存组 i → Cache 组 i，组内全相联。兼顾性能和复杂度

**Cache 替换算法**：
- **随机替换**：随机选择一块替换
- **FIFO**：替换最早进入 Cache 的块
- **LRU（最近最少使用）**：替换最久未被访问的块，命中率最高
- **LFU（最不经常使用）**：替换访问次数最少的块

**Cache 写策略**：
- **写直达（Write-Through）**：同时写入 Cache 和主存，保证一致性但慢
- **写回（Write-Back）**：只写 Cache，修改标记，块被替换时写回主存
- **写一次（Write-Once）**：第一次写用写直达，后续用写回

#### ***公式总结***

| 指标 | 公式 | 说明 |
|------|------|------|
| **命中率 H** | H = 命中次数 / 总访问次数 | 理想情况 H→1 |
| **缺失率** | 1 - H | 未命中比例 |
| **平均访问时间** | Tₐᵥ₉ = H×T_c + (1-H)×Tₘ | T_c 为 Cache 时间，Tₘ为主存时间 |
| **加速比** | S = Tₘ / Tₐᵥ₉ | 使用 Cache 后的速度提升倍数 |
| **Cache 容量** | S = 块大小 × 块数 | 典型值：32KB~8MB |
| **块冲突概率** | 直接映射 > 组相联映射 > 全相联映射 | |

**多级 Cache 平均访问时间**：
Tₐᵥ₉ = H₁×T₁ + (1-H₁)×H₂×T₂ + (1-H₁)×(1-H₂)×Tₘ

**Cache 地址划分（直接映射）**：
| 主存标记 | Cache 块号 | 块内地址 |
|---------|-----------|---------|
| t 位 | c 位 | b 位 |

#### ***案例分析***

**【2021 年真题】**
设读取一次 Cache 时间为 1ns，读取一次内存时间为 1000ns。若 CPU 访问 Cache 的命中率为 90%，求 CPU 读取一次数据的平均时间。

**解答**：
Tₐᵥ₉ = 0.9×1ns + 0.1×1000ns = 0.9ns + 100ns = 100.9ns

**【典型应用题】**
某计算机主存容量 64KB，Cache 容量 2KB，每个块 16 字节。求：
(1) Cache 可容纳多少块？
(2) 采用直接映射时，主存地址如何划分？

**解答**：
(1) Cache 块数 = 2KB / 16B = 2048 / 16 = 128 块
(2) 主存地址 16 位（64KB=2¹⁶）：
- 块内地址：4 位（16B=2⁴）
- Cache 块号：7 位（128 块 = 2⁷）
- 主存标记：16-4-7=5 位

**【多级 Cache 真题】**
某计算机的 Cache 分为两级，L1 Cache 访问时间 1ns，命中率 90%；L2 Cache 访问时间 10ns，命中率 95%（L1 未命中后）；主存访问时间 100ns。求平均访问时间。

**解答**：
Tₐᵥ₉ = 0.9×1 + 0.1×0.95×10 + 0.1×0.05×100
= 0.9 + 0.95 + 0.5 = 2.35ns

#### ***难题解析***

**【易错题】**
按照 Cache 地址映像的块冲突概率，从高到低排列正确的是：
A. 全相联映像 > 直接映像 > 组相联映像
B. 直接映像 > 组相联映像 > 全相联映像
C. 组相联映像 > 全相联映像 > 直接映像

**解析**：
- **直接映像**：每个主存块只能映射到唯一的 Cache 块位置，冲突概率最高
- **组相联映像**：每个主存块可映射到组内的任意位置，冲突概率中等
- **全相联映像**：每个主存块可映射到 Cache 的任意位置，冲突概率最低

**正确答案：B**

**易错点**：考生容易混淆"映射灵活性"与"冲突概率"的关系。映射越灵活（自由度越高），冲突概率越低。

**【拔高题·组相联映射】**
某 Cache 容量 64 块，采用 4 路组相联映射，每个块 32 字节，主存地址 32 位。求主存地址中标记字段、组索引字段和块内地址字段各多少位？

**解析**：
- 块内地址：log₂(32) = 5 位
- Cache 总组数 = 64 / 4 = 16 组
- 组索引：log₂(16) = 4 位
- 标记位：32 - 5 - 4 = 23 位

地址格式：| 标记(23位) | 组索引(4位) | 块内地址(5位) |

---

### 1.3 校验码

#### ***背景***

数据在传输或存储过程中可能因为噪声、介质缺陷等原因产生错误。校验码是一种通过增加冗余信息来检测或纠正错误的技术。常见的校验码有奇偶校验码、CRC（循环冗余校验）和汉明码等。

#### ***基础原理推导***

**奇偶校验**：
- 在编码中增加 1 位校验位，使编码中 1 的个数为奇数（奇校验）或偶数（偶校验）
- 码距变为 2，只能检测奇数个错误，无法定位和纠错

**CRC 校验**：
- 在原始信息位后追加若干校验位，使得追加后的信息能被生成多项式 G(x) 整除
- 接收方用同样的 G(x) 去除，余数为 0 则无错，否则重传
- CRC 只能检错，不能纠错

**汉明码**：
- 可以检测并纠正一位错误
- 校验位数量 k 满足：2ᵏ ≥ m + k + 1（m 为数据位位数）

#### ***公式总结***

| 编码类型 | 码距 | 检错能力 | 纠错能力 |
|---------|------|---------|---------|
| 奇偶校验 | 2 | 检奇数位错 | 无 |
| CRC | ≥2 | 取决于生成多项式 | 无 |
| 汉明码 | 3 | 检 2 位错 | 纠 1 位错 |

**CRC 编码步骤**：
1. 信息位后加 r 个 0（r = G(x) 阶数）
2. 模 2 除法（异或）得到余数
3. 余数替换末尾 r 个 0，得到 CRC 编码

#### ***案例分析***

**【CRC 真题】**
若数据信息为 1100，生成多项式为 X³+X+1（即 1011），求 CRC 编码。

**解答**：
1. 加 3 个 0：1100000
2. 模 2 除法：1100000 ÷ 1011，余数为 010
3. CRC 编码：1100010
**答案：A. 1100010**

---

## 2. 操作系统知识

### 2.1 PV 操作与进程同步互斥

#### ***背景***

PV 操作由荷兰计算机科学家 Dijkstra 于 1965 年提出，是解决并发进程同步与互斥问题的经典机制。"P"来自荷兰语"Proberen"（测试），"V"来自"Verhogen"（增加）。

**应用场景**：
- 操作系统进程调度
- 数据库事务并发控制
- 多线程编程（Java 的 synchronized、C++ 的 mutex）
- 分布式系统资源调度

**解决的核心问题**：用一种统一的、可证明正确的机制，解决并发环境下的资源竞争与执行顺序协调问题。

#### ***基础原理推导***

**信号量（Semaphore）**：一个特殊的整型变量，代表可用资源数量。

**P 操作（申请资源）**：
```
P(S):
    S = S - 1
    if S >= 0:
        进程继续执行（资源充足）
    else:
        进程阻塞，进入该信号量的等待队列
```

**V 操作（释放资源）**：
```
V(S):
    S = S + 1
    if S > 0:
        进程继续执行（无进程等待）
    else:
        从等待队列唤醒一个进程，然后继续执行
```

**核心逻辑**：
- **互斥信号量**：初值 = 1，用于保护临界区，保证互斥访问
- **同步信号量**：初值 = 资源数，用于协调进程间的执行顺序

**前趋图与 PV 操作**：
- 前趋图中每条箭头对应一个信号量
- 箭头起点（前趋进程）执行 V 操作（发信号）
- 箭头终点（后继进程）执行 P 操作（等信号）

**进程三态模型**：
- 运行态 →（时间片到）→ 就绪态
- 运行态 →（等待事件）→ 阻塞态
- 阻塞态 →（事件发生）→ 就绪态
- 就绪态 →（被调度）→ 运行态

#### ***公式总结***

| 类型 | 初值 | 含义 | 典型应用 |
|------|------|------|---------|
| **互斥信号量** | 1 | 临界资源的使用权 | 保护共享变量、设备 |
| **同步信号量（空）** | n | 空闲缓冲区数量 | 生产者消费者问题 |
| **同步信号量（满）** | 0 | 已填充缓冲区数量 | 生产者消费者问题 |

**信号量取值范围计算**：
- 初始值：S₀（可用资源数）
- 最小值：S₀ - n（n 为并发进程数）
- 最大值：S₀

**信号量最终值计算**：
```
最终值 = 初始值 - P操作成功次数 + V操作次数
```

#### ***案例分析***

**【2023 年真题】**
假设系统有 n（n≥5）个进程共享资源 R，且资源 R 的可用数为 5。若采用 PV 操作，则相应的信号量 S 的取值范围应为？

**解答**：
- 初始值 S₀ = 5
- 最好情况：无进程申请资源 → S = 5
- 最坏情况：n 个进程都申请了，其中 5 个获得资源，n-5 个等待 → S = 5 - n
- 取值范围：-(n-5) ~ 5

**【经典生产者消费者问题】**
一个仓库可存放 n 件产品。信号量设置：mutex=1（互斥），empty=n（空位数），full=0（产品数）。

```
生产者流程：          消费者流程：
P(empty)             P(full)
P(mutex)             P(mutex)
放入产品              取出产品
V(mutex)             V(mutex)
V(full)              V(empty)
```

**要点**：P(empty) 和 P(mutex) 的顺序不能颠倒，否则可能导致死锁（先锁住仓库再等空位，若仓库已满则永远等不到空位）。

#### ***难题解析***

**【2022 年前趋图真题】**
进程 P1、P2、P3、P4、P5 的前趋图关系为：P1→(S1)P2→(S3)P4，P1→(S2)P3→(S4)P5，P2、P3 都完成后才能执行 P4？不，按图。问：P2 执行完后应执行什么操作？P4 开始执行前应执行什么操作？

**解析**：
- P2 有两个后继：P4（通过 S3）和 P3（通过 S2）
- 所以应执行：V(S2) 和 V(S3)
- P4 等待 P2 的 S3 信号
- 所以应执行：P(S3)

**易错点**：一个进程有多个后继时，需要对每个后继分别执行 V 操作；一个进程有多个前趋时，需要对每个前趋分别执行 P 操作。

**【经典同步问题·读者写者问题】**
多个读者可以同时读，但写者必须独占。用 PV 操作实现。

**信号量**：rw_mutex=1（读写互斥），count_mutex=1（保护读者计数），read_count=0

```
写者流程：             读者流程：
P(rw_mutex)           P(count_mutex)
写操作                 read_count++
V(rw_mutex)           if read_count==1: P(rw_mutex)
                      V(count_mutex)
                      读操作
                      P(count_mutex)
                      read_count--
                      if read_count==0: V(rw_mutex)
                      V(count_mutex)
```

---

### 2.2 死锁与银行家算法

#### ***背景***

死锁问题于 1965 年由 Dijkstra 在研究银行家问题时首次系统阐述。在多道程序环境中，多个进程因竞争资源而造成的一种僵局：每个进程都占有部分资源，同时等待其他进程占有的资源，导致所有进程都无法继续推进。

**应用场景**：
- 操作系统进程资源分配
- 数据库事务死锁检测
- 分布式系统资源调度
- 多线程应用程序设计

**解决的核心问题**：如何在资源有限的情况下，避免或解决多个进程因循环等待资源而导致的系统停滞。

#### ***基础原理推导***

**死锁产生的四个必要条件（Coffman 条件）**：
1. **互斥条件**：资源一次只能被一个进程使用
2. **请求与保持条件**：进程已占有资源，又请求新资源
3. **不剥夺条件**：进程已获得的资源不能被强行剥夺
4. **循环等待条件**：存在进程-资源的循环等待链

**死锁处理策略**：
- **死锁预防**：破坏四个必要条件之一
- **死锁避免**：银行家算法，动态检查确保安全
- **死锁检测**：允许死锁发生，定期检测
- **死锁解除**：剥夺资源、撤销进程

**银行家算法核心思想**：
银行家在放贷时，必须保证能够收回所有贷款。类比到操作系统：银行家 = OS，贷款 = 资源分配，客户 = 进程。

**进程资源图化简方法**：
1. 先看系统还剩多少资源没分配
2. 找出不阻塞的进程（请求的资源还有剩余）
3. 把不阻塞的进程的所有边去掉，回收其占用的资源
4. 重复直到所有进程都变成孤立点 → 可化简（无死锁）
5. 若存在无法化简的进程 → 死锁

#### ***公式总结***

**死锁临界资源计算**：
```
不发生死锁的最小资源数 = n × (R - 1) + 1
```
其中：n = 进程数，R = 每个进程需要的资源数

**银行家算法安全性检查**：
1. 计算每个进程的剩余需求：Need = Max - Allocation
2. 寻找 Need ≤ Available 的进程
3. 假设该进程完成，释放资源：Available = Available + Allocation
4. 重复直到所有进程都能完成（安全）或找不到满足条件的进程（不安全）

#### ***案例分析***

**【2023 年真题】**
某系统中有 3 个并发进程竞争资源 R，每个进程都需要 5 个 R。那么至少需要多少个 R，才能保证系统不会发生死锁？

**解答**：
- 最坏情况：每个进程都获得了 4 个资源（差 1 个就能完成）
- 死锁临界点：3 × 4 = 12 个资源
- 再加 1 个资源，就能让至少一个进程完成，释放资源
- 答案：12 + 1 = 13 个

**【银行家算法真题】**
系统有三类资源 R1、R2、R3，可用资源数为 (10,5,3)。5 个进程的资源情况如下：

| 进程 | Allocation | Need |
|------|-----------|------|
| P1 | (0,1,0) | (7,4,3) |
| P2 | (2,0,0) | (1,2,2) |
| P3 | (3,0,2) | (6,0,0) |
| P4 | (2,1,1) | (0,1,1) |
| P5 | (0,0,2) | (4,3,1) |

**解答**：
1. Available = (10,5,3) - (7,2,5) = (3,3,-2)？不对。(0+2+3+2+0, 1+0+0+1+0, 0+0+2+1+2) = (7,2,5)... 说明可用资源不足
2. 若 Available = (2,0,1)，安全序列为 P2→P4→P5→P1→P3 或类似序列

#### ***难题解析***

**【拔高题】**
某系统有 n 个进程共享 6 台打印机，每个进程最多需要 2 台打印机。问 n 最大为多少时，系统一定不会发生死锁？

**解析**：
- 每个进程最多需要 2 台，最坏情况每个进程都获得 1 台，都在等待第 2 台
- 死锁临界点：n × 1 = 6 → n = 6 时可能死锁
- 要保证不死锁：n × (2-1) + 1 ≤ 6 → n + 1 ≤ 6 → n ≤ 5
- 验证：n=5 时，最坏情况 5 个进程各 1 台，还剩 1 台，可让一个进程完成，释放 2 台，连锁推进

**答案：n 最大为 5**

**【易错题】**
若系统中有 5 台绘图仪，有多个进程均需要使用 2 台。规定每个进程一次仅允许申请一台，则至多允许多少个进程参与竞争而不会发生死锁？

**解析**：
- 每个进程需要 2 台，最坏情况每个获得 1 台
- 5 = n × 1 → n = 5 时，全部持有 1 个，都在等第 2 个，死锁
- 5 = n × 1 + 1 → n = 4 时还有 1 台剩余，至少一个进程可完成
- 答案：4 个

---

## 3. 数据库系统

### 3.1 数据库范式（1NF-2NF-3NF-BCNF）

#### ***背景***

数据库规范化理论由 E.F.Codd 于 1970 年提出，是关系数据库设计的基石。在规范化理论提出之前，数据库设计缺乏科学指导，常常出现数据冗余、插入异常、删除异常、更新异常等问题。

**应用场景**：
- 关系数据库表结构设计
- 数据库逻辑建模
- 遗留系统数据库重构
- OLTP 系统表结构优化

**解决的核心问题**：通过逐级消除关系模式中的不合适的数据依赖，解决数据冗余和各种操作异常，保证数据库设计的质量。

#### ***基础原理推导***

**函数依赖定义**：
如果对于关系 R 中任意两个元组，只要属性 X 的值相同，属性 Y 的值就一定相同，则称 X 函数确定 Y，记作 X→Y。

**依赖类型**：
- **完全函数依赖**：X→Y，且 X 的任何真子集都不能确定 Y
- **部分函数依赖**：X→Y，但 X 的某个真子集也能确定 Y（只存在于复合主键）
- **传递函数依赖**：X→Y，Y→Z，且 Y 不能确定 X，Y 不是 X 的子集

**范式逐级升级逻辑**：
```
1NF（原子性）
  ↓ 消除部分函数依赖
2NF（完全依赖）
  ↓ 消除传递函数依赖
3NF（非主属性不传递依赖于码）
  ↓ 消除主属性对码的部分和传递依赖
BCNF（每一个决定因素都包含码）
```

**候选键的求解（属性闭包法）**：
- 从给定函数依赖集 F 出发，求属性集 X 的闭包 X⁺
- 若 X⁺ 包含所有属性，则 X 是候选键
- 求闭包算法：反复应用函数依赖，直到不再增长

#### ***公式总结***

| 范式 | 核心要求 | 解决的问题 |
|------|---------|-----------|
| **1NF** | 每一列都是不可再分的原子数据项 | 消除表中套表 |
| **2NF** | 1NF + 所有非主属性完全依赖于整个主键 | 消除部分函数依赖 |
| **3NF** | 2NF + 不存在非主属性对码的传递依赖 | 消除传递函数依赖 |
| **BCNF** | 3NF + 每一个决定因素都包含候选码 | 消除主属性的部分/传递依赖 |

**判断口诀**：
- 1NF：属性原子不可分
- 2NF：非主属性都完全依赖主键（不依赖主键的一部分）
- 3NF：非主属性只依赖主键（不依赖其他非主属性）
- BCNF：只有主键能决定其他属性（任何决定因素都是码）

#### ***案例分析***

**【经典学生表案例】**
原始设计（不满足 2NF）：学生(学号, 姓名, 系号, 系主任, 课程号, 成绩)，主键：(学号, 课程号)

**依赖分析**：
- (学号, 课程号) → 成绩 ✓（完全依赖）
- 学号 → 姓名, 系号 ✗（部分依赖，只依赖主键的一部分）
- 系号 → 系主任 ✗（传递依赖）

**分解为 2NF**：
- 学生(学号, 姓名, 系号, 系主任)，主键：学号
- 选课(学号, 课程号, 成绩)，主键：(学号, 课程号)

**分解为 3NF**：
- 学生(学号, 姓名, 系号)，主键：学号
- 系(系号, 系主任)，主键：系号
- 选课(学号, 课程号, 成绩)，主键：(学号, 课程号)

**【2022 年真题】**
给定关系模式 R(A,B,C,D,E)，函数依赖集 F={A→B, B→C, C→D, D→E}。问 R 最高满足第几范式？

**解答**：
- 候选码：A（因为 A→B→C→D→E，A 可决定所有属性）
- 非主属性：B,C,D,E
- 检查 2NF：主键是单属性 A，不存在部分依赖 ✓
- 检查 3NF：存在传递依赖 A→B→C，不满足 3NF
- 结论：最高满足 2NF

#### ***难题解析***

**【BCNF 难题】**
关系模式 STJ(S,T,J)，其中 S=学生，T=教师，J=课程。语义：每个教师只教一门课；每门课有若干教师；某学生选定某门课，就对应一个固定的教师。函数依赖：(S,J)→T，(S,T)→J，T→J。问最高满足第几范式？

**解析**：
1. **找候选码**：
   - (S,J) 可以决定 T，是候选码
   - (S,T) 可以决定 J，是候选码
   - 主属性：S,T,J（三个都是主属性！）
   - 非主属性：无

2. **检查 3NF**：
   - 3NF 要求：不存在非主属性对码的部分和传递依赖
   - 因为没有非主属性，所以自动满足 3NF ✓

3. **检查 BCNF**：
   - BCNF 要求：每一个决定因素都包含码
   - 看 T→J 这个依赖：决定因素是 T
   - T 是不是候选码？不是（T 只能决定 J，不能决定 S）
   - 所以不满足 BCNF ✗

**结论**：最高满足 3NF，不满足 BCNF

**易错点**：很多考生以为 3NF 之后一定是 BCNF，或者认为没有非主属性就满足 BCNF。实际上 3NF 只限制"非主属性"，而 BCNF 对"主属性"也有限制。

---

### 3.2 数据库设计

#### ***背景***

数据库设计是信息系统开发的核心环节，设计质量直接影响系统的性能、可维护性和可扩展性。

#### ***基础原理推导***

**数据库设计步骤**：
1. **需求分析**：产出物包括数据流图、数据字典、需求说明书。获得信息要求、处理要求、系统要求
2. **概念结构设计**：设计 E-R 图。合并分 E-R 图时注意三类冲突：
   - 属性冲突：同一属性在不同分图中类型/长度不同
   - 命名冲突：同名异义或异名同义
   - 结构冲突：同一对象在一个图中是实体，在另一个图中是属性
3. **逻辑结构设计**：将 E-R 图转换为关系模式，确定完整性约束和用户视图
4. **物理设计**：确定数据分布、存储结构和访问方式
5. **数据库实施阶段**：建立数据库，编制调试应用程序
6. **数据库运行和维护阶段**

**三级模式结构**：
- **外模式**（用户视图）：与应用程序相关
- **模式**（概念模式）：所有用户的公共数据视图
- **内模式**（存储模式）：数据的物理存储方式

**两级映像**：
- 外模式/模式映像：逻辑独立性
- 模式/内模式映像：物理独立性

#### ***案例分析***

**【真题】**
在数据库系统中，数据库的视图、基本表和存储文件的结构分别与（）对应。

**解答**：
视图 → 外模式，基本表 → 模式，存储文件 → 内模式
**答案：C. 外模式、模式、内模式**

---

## 4. 嵌入式技术

### ***背景***

嵌入式系统是以应用为中心，以计算机技术为基础，软硬件可裁剪，适应应用系统对功能、可靠性、成本、体积和功耗有严格要求的专用计算机系统。

### ***基础原理推导***

**嵌入式系统特点**：
1. 专用性强，针对特定应用设计
2. 资源受限（CPU/内存/存储）
3. 实时性要求高
4. 可靠性要求高
5. 低功耗设计

**嵌入式系统初始化过程**：
片级初始化 → 板级初始化 → 系统初始化（自底向上，从硬件到软件）

**常见的嵌入式操作系统**：
- Linux（裁剪版）、VxWorks、μC/OS-II、FreeRTOS、RT-Thread

### ***案例分析***

**【真题】**
嵌入式系统初始化过程按照自底向上、从硬件到软件的次序依次为：

**答案**：片级初始化 → 板级初始化 → 系统初始化

---

## 5. 计算机网络

### 5.1 OSI 七层模型与 TCP/IP 协议族

#### ***背景***

计算机网络的核心问题是解决不同设备之间的通信问题。OSI 七层模型（1984年）是一个标准化的网络通信参考模型，而 TCP/IP 协议族是实际工业标准。

#### ***基础原理推导***

**OSI 七层模型**（从下到上）：

| 层次 | 功能 | 数据单位 | 典型协议 | 设备 |
|------|------|---------|---------|------|
| 物理层 | 透明传输比特流 | 比特 | RS-232、RJ-45 | 中继器、集线器 |
| 数据链路层 | 帧传输、差错控制 | 帧 | HDLC、PPP、以太网 | 交换机、网桥 |
| 网络层 | 路由选择、拥塞控制 | 分组 | IP、ICMP、ARP | 路由器 |
| 传输层 | 端到端可靠传输 | 报文段 | TCP、UDP | 网关 |
| 会话层 | 建立/管理/终止会话 | - | RPC、SQL | 网关 |
| 表示层 | 数据格式转换、加密压缩 | - | JPEG、ASCII | 网关 |
| 应用层 | 为用户提供服务 | 数据 | HTTP、FTP、SMTP | 网关 |

**TCP 与 UDP 对比**：
| 特性 | TCP | UDP |
|------|-----|-----|
| 连接方式 | 面向连接 | 无连接 |
| 可靠性 | 可靠传输 | 不可靠 |
| 速度 | 慢 | 快 |
| 应用场景 | 文件传输、Web | 视频直播、DNS |

#### ***案例分析***

**【真题】**
在 OSI 参考模型中，（）在物理层提供连接服务，（）在数据链路层提供连接服务。

**解答**：物理层设备：中继器、集线器；数据链路层设备：交换机、网桥

---

### 5.2 核心协议详解（ARP/RARP/ICMP/IGMP）

#### ***背景***

TCP/IP 协议族中，网络层除了核心的 IP 协议外，还有多个辅助协议负责地址解析、差错控制和组播管理。

#### ***基础原理推导***

**ARP（地址解析协议）**：
- **功能**：已知目标 IP 地址，查询其 MAC 地址
- **工作原理**：
  1. 主机广播 ARP 请求："谁的 IP 是 X.X.X.X？"
  2. 目标主机单播回复 ARP 响应："我的 MAC 是 XX:XX:XX:XX:XX:XX"
  3. 获取结果后存入本地 ARP 缓存（有生存时间 TTL）
- **ARP 欺骗**：攻击者发送伪造 ARP 响应，将网关 MAC 指向攻击者，实现中间人攻击

**RARP（逆地址解析协议）**：
- **功能**：已知 MAC 地址，查询其 IP 地址
- **应用场景**：无盘工作站启动时，通过 RARP 向服务器获取 IP 地址
- **现状**：已被 BOOTP/DHCP 取代

**ICMP（互联网控制报文协议）**：
- **功能**：传递网络层的差错报告和诊断信息
- **典型应用**：
  - `ping` 命令：发送 ICMP Echo Request，接收 Echo Reply，检测网络连通性
  - `tracert/traceroute`：利用 ICMP 超时报文追踪路由路径
  - 目标不可达（Type 3）、超时（Type 11）、参数问题
- **ICMP 洪水攻击**：大量 ICMP Echo Request 耗尽目标带宽（Smurf 攻击变种）

**IGMP（互联网组管理协议）**：
- **功能**：管理 IP 组播组的成员关系
- **工作原理**：
  1. 主机向路由器发送 IGMP 报告，申请加入某个组播组
  2. 路由器定期查询组成员状态
  3. 主机离开组播组时发送离开报文
- **版本**：IGMPv1/v2/v3，v2 增加了离开机制，v3 支持源过滤

#### ***公式总结***

| 协议 | 层次 | 协议号 | 方向 | 核心机制 |
|------|------|--------|------|---------|
| ARP | 网络层（链路层之上） | 0x0806 | IP→MAC | 广播请求，单播回复 |
| RARP | 网络层（链路层之上） | 0x8035 | MAC→IP | 已被 DHCP 取代 |
| ICMP | 网络层（IP 之上） | 1 | 差错/诊断 | 查询 + 差错报告 |
| IGMP | 网络层（IP 之上） | 2 | 组播管理 | 加入/离开/查询 |

---

### 5.3 传输层协议（TCP/UDP）

#### ***基础原理推导***

**TCP（传输控制协议）**：
- **面向连接**：三次握手建立连接，四次挥手释放连接
- **可靠传输机制**：
  - **确认重传**：接收方确认收到的段，发送方超时未收到 ACK 则重传
  - **序号机制**：每个字节一个序号，保证有序到达
  - **流量控制**：滑动窗口协议，接收方通过窗口大小告知发送方剩余缓冲区
  - **拥塞控制**：慢启动（Slow Start）、拥塞避免（Congestion Avoidance）、快速重传（Fast Retransmit）、快速恢复（Fast Recovery）
- **三次握手**：
  1. 客户端 → 服务端：SYN（seq=x）
  2. 服务端 → 客户端：SYN+ACK（seq=y, ack=x+1）
  3. 客户端 → 服务端：ACK（seq=x+1, ack=y+1）
- **四次挥手**：
  1. 客户端 → 服务端：FIN
  2. 服务端 → 客户端：ACK
  3. 服务端 → 客户端：FIN
  4. 客户端 → 服务端：ACK

**UDP（用户数据报协议）**：
- **无连接**：发送前无需建立连接
- **不可靠**：不保证送达、不保证顺序
- **开销小**：首部仅 8 字节（TCP 首部 20 字节）
- **适用场景**：DNS 查询、DHCP、视频直播、VoIP、在线游戏

**TCP 端口号范围**：
- 0-1023：周知端口（Well-Known Ports）
- 1024-49151：注册端口
- 49152-65535：动态/私有端口

#### ***案例分析***

**【真题】**
TCP 协议采用（）机制来实现流量控制。

**解答**：滑动窗口（Sliding Window）。接收方在 TCP 首部的窗口字段中告知发送方当前可用缓冲区大小，发送方据此调整发送速率。

**【真题】**
TCP 三次握手中，第二次握手时服务端发送的报文段中（）标志位被置 1。

**解答**：SYN 和 ACK 同时被置 1（SYN=1, ACK=1）。

---

### 5.4 应用层协议（FTP/HTTP/SMTP/POP3/DHCP/DNS）

#### ***基础原理推导***

**FTP（文件传输协议）**：
- **端口**：控制连接 21/TCP，数据连接 20/TCP
- **模式**：
  - **主动模式（PORT）**：服务器主动连接客户端的数据端口
  - **被动模式（PASV）**：服务器开放随机端口，客户端主动连接（适用于客户端有防火墙的场景）
- **特点**：明文传输用户名和密码（不安全），支持匿名访问

**HTTP/HTTPS（超文本传输协议）**：
- **端口**：HTTP 80/TCP，HTTPS 443/TCP
- **HTTP 版本**：
  | 版本 | 特点 |
  |------|------|
  | HTTP/1.0 | 短连接，每次请求新建 TCP 连接 |
  | HTTP/1.1 | 持久连接（Keep-Alive）、管道化、Host 头 |
  | HTTP/2.0 | 多路复用、二进制分帧、头部压缩、服务器推送 |
  | HTTP/3.0 | 基于 QUIC（UDP），零 RTT 建立连接 |
- **请求方法**：GET（读取）、POST（创建）、PUT（更新）、DELETE（删除）、HEAD（仅获取响应头）
- **状态码**：
  | 范围 | 含义 | 示例 |
  |------|------|------|
  | 1xx | 信息性 | 101 Switching Protocols |
  | 2xx | 成功 | 200 OK、201 Created |
  | 3xx | 重定向 | 301 Moved Permanently、302 Found |
  | 4xx | 客户端错误 | 400 Bad Request、404 Not Found |
  | 5xx | 服务器错误 | 500 Internal Server Error、503 Service Unavailable |

**SMTP（简单邮件传输协议）**：
- **端口**：25/TCP（默认）、465/TCP（SSL）、587/TCP（TLS）
- **功能**：用于邮件发送（客户端→服务器、服务器→服务器）
- **特点**：纯 ASCII 协议，简单命令驱动（HELO、MAIL FROM、RCPT TO、DATA）

**POP3（邮局协议第 3 版）**：
- **端口**：110/TCP
- **功能**：将邮件从服务器下载到本地阅读
- **特点**：下载后通常删除服务器上的副本（可配置保留），不支持服务器端文件夹管理

**IMAP（互联网消息访问协议）**：
- **端口**：143/TCP
- **功能**：在服务器端管理邮件，支持多文件夹
- **与 POP3 对比**：IMAP 更适合多设备场景，邮件保留在服务器

**DHCP（动态主机配置协议）**：
- **端口**：67/UDP（服务端）、68/UDP（客户端）
- **工作流程（DORA）**：
  1. **Discover**：客户端广播 DHCP Discover（寻找 DHCP 服务器）
  2. **Offer**：服务器回复 DHCP Offer（提供 IP 地址、子网掩码、网关、DNS）
  3. **Request**：客户端广播 DHCP Request（确认选择该配置）
  4. **Ack**：服务器回复 DHCP Ack（确认生效）
- **租约**：IP 地址有使用期限（租期），过半时续约
- **DHCP 中继**：跨子网使用 DHCP，需要中继代理转发广播

**DNS（域名系统）**：
- **端口**：53/UDP（查询）、53/TCP（区域传输）
- **域名结构**：根域 → 顶级域（.com/.cn/.org）→ 二级域 → 子域
- **解析过程**（递归查询）：
  1. 客户端向本地 DNS 服务器查询
  2. 本地 DNS 向根 DNS 查询顶级域服务器地址
  3. 根 DNS 返回顶级域服务器地址
  4. 本地 DNS 向顶级域 DNS 查询权威 DNS 地址
  5. 本地 DNS 向权威 DNS 查询目标 IP
  6. 本地 DNS 将结果返回客户端并缓存
- **记录类型**：A（IPv4 地址）、AAAA（IPv6 地址）、CNAME（别名）、MX（邮件交换）、NS（域名服务器）

#### ***案例分析***

**【真题】**
DNS 的解析方式有（）和迭代解析两种。

**解答**：递归解析。客户端到本地 DNS 服务器通常使用递归解析，本地 DNS 到其他 DNS 服务器使用迭代解析。

**【真题】**
当 DHCP 服务器和客户端不在同一个子网时，需要配置（）设备实现 DHCP 中继。

**解答**：路由器或三层交换机。DHCP 中继代理将客户端的广播 Discover 报文以单播方式转发给 DHCP 服务器。

---

### 5.5 交换与路由技术

#### ***背景***

交换和路由是网络中两种核心的数据转发方式。交换发生在数据链路层（基于 MAC 地址），路由发生在网络层（基于 IP 地址）。

#### ***基础原理推导***

**交换技术（数据链路层）**：

| 交换方式 | 工作原理 | 延迟 | 错误检测 |
|---------|---------|------|---------|
| **直通交换** | 读到目标 MAC 立即转发 | 低 | 无 |
| **存储转发** | 完整接收帧再转发 | 高 | 有（CRC 校验） |
| **无碎片转发** | 读取前 64 字节后转发 | 中 | 过滤碎片帧 |

**VLAN（虚拟局域网）**：
- **原理**：通过交换机端口划分逻辑隔离的广播域
- **标记**：IEEE 802.1Q 标准，在以太网帧头插入 4 字节 VLAN Tag（12 位 VLAN ID）
- **优势**：隔离广播域、提高安全性、简化管理

**路由技术（网络层）**：

**路由算法分类**：
- **静态路由**：手动配置，适合小型网络
- **动态路由**：
  - **距离矢量**（RIP）：基于跳数，每 30 秒广播路由表，最大跳数 15
  - **链路状态**（OSPF）：基于链路开销，维护全网拓扑数据库，收敛快
  - **混合型**（BGP）：路径矢量协议，互联网核心路由协议

| 协议 | 类型 | 度量 | 收敛速度 | 适用范围 |
|------|------|------|---------|---------|
| RIP | 距离矢量 | 跳数（≤15） | 慢 | 小型网络 |
| OSPF | 链路状态 | 带宽/延迟 | 快 | 大型企业网 |
| BGP | 路径矢量 | AS 路径 | 中等 | 互联网骨干 |

#### ***案例分析***

**【真题】**
以下路由协议中，基于链路状态算法的是（）。
A. RIP  B. OSPF  C. BGP  D. IGRP

**解答**：B。OSPF 基于链路状态算法（LSA 泛洪 + SPF 计算），RIP 和 IGRP 是距离矢量，BGP 是路径矢量。

---

### 5.6 传输介质与通信方式

#### ***基础原理推导***

**传输介质**：

| 介质类型 | 速率 | 距离 | 抗干扰 | 成本 | 应用场景 |
|---------|------|------|--------|------|---------|
| **双绞线**（Cat5e） | 1000Mbps | 100m | 中等 | 低 | 局域网最常用 |
| **双绞线**（Cat6） | 10Gbps | 55m | 较好 | 中 | 高速局域网 |
| **光纤**（单模） | 10Gbps~100Gbps | 40km+ | 强 | 高 | 广域网、骨干网 |
| **光纤**（多模） | 1Gbps~10Gbps | 550m | 强 | 中 | 数据中心 |
| **无线**（Wi-Fi 6） | 9.6Gbps | 室内 30m | 弱 | 中 | 移动接入 |
| **无线**（5G） | 20Gbps | 1-10km | 弱 | 高 | 移动广域网 |

**双绞线线序（T568B）**：白橙-橙-白绿-蓝-白蓝-绿-白棕-棕
**交叉线 vs 直通线**：
- **直通线**：两端线序相同，连接不同类型设备（PC↔交换机）
- **交叉线**：两端线序不同，连接同类型设备（PC↔PC、交换机↔交换机）

**通信方式分类**：

| 分类维度 | 类型 | 特点 | 示例 |
|---------|------|------|------|
| **方向性** | 单工 | 单向传输 | 广播电视 |
| | 半双工 | 轮流双向 | 对讲机 |
| | 全双工 | 同时双向 | 电话、以太网 |
| **同步方式** | 同步通信 | 时钟同步，效率高 | 数字通信 |
| | 异步通信 | 起止位，无需同步时钟 | RS-232 |
| **交换方式** | 电路交换 | 独占链路，实时性好 | PSTN 电话网 |
| | 报文交换 | 存储转发，无连接 | 电报网络 |
| | 分组交换 | 虚电路/数据报 | IP 网络（互联网） |

**分组交换两种方式**：
- **虚电路**：建立逻辑连接，分组沿相同路径传输（如 ATM、MPLS）
- **数据报**：每个分组独立路由，路径可以不同（如 IP 协议）

#### ***案例分析***

**【真题】**
在下列通信方式中，（）的传输延迟最小。

**解答**：电路交换。因为电路交换在通信前建立专用链路，通信过程中不需要存储转发，传输延迟主要由传播延迟决定。

---

### 5.7 IP 地址与子网规划

#### ***基础原理推导***

**IP 地址分类（IPv4）**：

| 类别 | 首bit | 网络位 | 主机位 | 地址范围 | 默认子网掩码 | 最大网络数 | 最大主机数 |
|------|-------|--------|--------|---------|------------|-----------|-----------|
| A 类 | 0 | 8位 | 24位 | 1.0.0.0 ~ 126.255.255.255 | 255.0.0.0 | 126（2⁷-2） | 16,777,214 |
| B 类 | 10 | 16位 | 16位 | 128.0.0.0 ~ 191.255.255.255 | 255.255.0.0 | 16,384（2¹⁴） | 65,534 |
| C 类 | 110 | 24位 | 8位 | 192.0.0.0 ~ 223.255.255.255 | 255.255.255.0 | 2,097,152（2²¹） | 254 |
| D 类 | 1110 | - | - | 224.0.0.0 ~ 239.255.255.255 | - | 组播地址 | - |
| E 类 | 1111 | - | - | 240.0.0.0 ~ 255.255.255.255 | - | 保留地址 | - |

**私有地址范围**（RFC 1918）：
- A 类：10.0.0.0/8（10.0.0.0 ~ 10.255.255.255）
- B 类：172.16.0.0/12（172.16.0.0 ~ 172.31.255.255）
- C 类：192.168.0.0/16（192.168.0.0 ~ 192.168.255.255）

**特殊地址**：
- 127.0.0.0/8：回环地址（localhost），用于本地测试
- 169.254.0.0/16：APIPA 自动私有地址（DHCP 失败时自分配）
- 0.0.0.0：任意地址（默认路由）
- 255.255.255.255：有限广播地址

**子网划分**：
```
子网数 = 2^借位数
每子网主机数 = 2^(主机位数) - 2
子网掩码 = 原掩码 + 借位对应的掩码值
```

**CIDR（无类域间路由）**：
- 格式：IP 地址 / 前缀长度（如 192.168.1.0/24）
- **聚合**：将多个连续子网聚合成一个超网，减少路由表项
- 示例：192.168.0.0/23 覆盖 192.168.0.0 ~ 192.168.1.255

**IPv6 要点**：
- **地址长度**：128 位（IPv4 为 32 位）
- **表示法**：8 组 16 位十六进制数，如 `2001:0db8:85a3:0000:0000:8a2e:0370:7334`
- **简写规则**：每组前导零可省略，连续全零组可用 `::` 代替一次
- **地址类型**：单播（Unicast）、组播（Multicast）、任播（Anycast）
- **无状态地址自动配置（SLAAC）**：设备根据路由器通告自动生成地址
- **与 IPv4 区别**：无广播地址、无 NAT 需要、内置 IPsec 支持

#### ***案例分析***

**【真题】**
某公司申请了一个 C 类地址 200.200.200.0，需要划分 6 个子网，求子网掩码及各子网可用地址范围。

**解答**：
- 6 个子网需要 3 位（2³=8 ≥ 6）
- 子网掩码：255.255.255.224（/27）
- 每子网主机数：2⁵-2 = 30
- 子网范围：0、32、64、96、128、160、192、224
- 示例：200.200.200.32/27 可用地址为 200.200.200.33 ~ 200.200.200.62

**【真题】**
IPv6 地址 2001:0000:0000:00B3:0000:0000:0000:12AB 的最简形式是？

**解答**：2001::B3:0:0:0:12AB 或 2001:0:0:B3::12AB

**解析**：`::` 只能使用一次，将最长连续全零段替换。注意 B3 段是 00B3，简写为 B3。

---

### 5.8 网络规划与结构化布线

#### ***背景***

网络规划是按照层次化、模块化的思想设计企业网络架构，确保网络的可用性、可扩展性和可管理性。

#### ***基础原理推导***

**三层网络模型**：

| 层次 | 功能 | 设备 | 特点 |
|------|------|------|------|
| **核心层** | 高速数据转发 | 高端路由器/交换机 | 冗余设计、高吞吐量、QoS |
| **汇聚层** | 策略控制、VLAN 路由 | 三层交换机 | 广播域划分、路由聚合、访问控制 |
| **接入层** | 终端用户接入 | 二层交换机 | 端口安全、VLAN 划分、PoE |

**设计原则**：
- **核心层**：不进行任何策略操作（ACL、NAT），只做高速转发
- **汇聚层**：实施安全策略、路由聚合、VLAN 间路由
- **接入层**：提供端口密度，支持广播域隔离

**结构化布线系统（6 个子系统）**：
1. **工作区子系统**：信息插座到终端设备
2. **水平子系统**：楼层配线间到信息插座
3. **管理子系统**：配线架、跳线管理
4. **垂直干线子系统**：各楼层配线间到主配线间的干线线缆
5. **设备间子系统**：核心设备集中放置区域
6. **建筑群子系统**：多栋建筑间的互连线缆

#### ***案例分析***

**【真题】**
在三层网络架构中，通常将（）功能放在汇聚层实现。
A. 高速转发  B. 终端接入  C. VLAN 间路由  D. 供电

**解答**：C。VLAN 间路由通常在汇聚层的三层交换机上实现。高速转发是核心层的职责，终端接入是接入层的职责。

---

### 5.9 存储技术与 RAID

#### ***背景***

随着数据量的爆炸增长，存储系统成为企业 IT 架构的核心组成部分。RAID 技术通过将多块磁盘组合成一个逻辑单元，提供更高的性能和可靠性。

#### ***基础原理推导***

**RAID 级别对比**：

| 级别 | 最少磁盘 | 容错能力 | 可用容量 | 读写性能 | 应用场景 |
|------|---------|---------|---------|---------|---------|
| **RAID 0** | 2 | 无 | 100% | 读↑ 写↑ | 临时数据、高性能缓存 |
| **RAID 1** | 2 | 1 块磁盘故障 | 50% | 读↑ 写↓ | 系统盘、日志盘 |
| **RAID 3** | 3 | 1 块磁盘故障 | (n-1)/n | 读↑ 写↓（有奇偶校验盘瓶颈） | 大文件连续读写 |
| **RAID 5** | 3 | 1 块磁盘故障 | (n-1)/n | 读↑ 写中 | 通用存储、文件服务器 |
| **RAID 10** | 4 | 每组镜像 1 块 | 50% | 读↑↑ 写↑ | 高性能数据库 |

**RAID 核心原理**：
- **条带化（Striping）**：数据分块并行写入多块磁盘，提高性能
- **镜像（Mirroring）**：数据完全复制到另一块磁盘，提高可靠性
- **奇偶校验（Parity）**：通过异或运算计算校验数据，节省容量

**网络存储架构**：

| 类型 | 全称 | 协议 | 传输单位 | 特点 | 适用场景 |
|------|------|------|---------|------|---------|
| **DAS** | 直连存储 | SATA/SAS/FC | 块 | 直接连接服务器，共享困难 | 单服务器 |
| **NAS** | 网络附加存储 | TCP/IP（CIFS/NFS） | 文件 | 基于文件共享，配置简单 | 文件共享、备份 |
| **SAN** | 存储区域网络 | FC/iSCSI | 块 | 高速专用网络，性能最佳 | 数据库、虚拟化 |

**DAS vs NAS vs SAN 选择**：
- **DAS**：低成本、低复杂度，适合单机环境
- **NAS**：跨平台文件共享方便，适合办公协作
- **SAN**：高性能块级存储，适合核心业务系统

#### ***案例分析***

**【真题】**
以下 RAID 级别中，兼顾性能和容错能力，且磁盘利用率最高的是（）。
A. RAID 0  B. RAID 1  C. RAID 5  D. RAID 10

**解答**：C。RAID 5 使用 1 块磁盘的容量存储奇偶校验信息，n 块磁盘可用 (n-1)/n 的容量，同时支持单盘故障容错。

**【真题】**
文件共享服务器最合适的存储方案是（）。
A. DAS  B. NAS  C. SAN  D. FC-SAN

**解答**：B。NAS 基于文件协议（CIFS/NFS），配置简单，天然支持多客户端文件共享。

---

### 5.10 其他网络技术（NAT/VLAN/VPN）

#### ***基础原理推导***

**NAT（网络地址转换）**：
- **功能**：将私有 IP 地址映射为公网 IP 地址，缓解 IPv4 地址枯竭
- **类型**：
  | 类型 | 说明 | 特点 |
  |------|------|------|
  | **静态 NAT** | 一对一映射（私有IP ↔ 公网IP） | 固定映射，外部可主动访问 |
  | **动态 NAT** | 多对多映射（私有IP池 ↔ 公网IP池） | 同时在线数受限于公网IP数量 |
  | **PAT/NPAT** | 多对一映射（端口多路复用） | 最常用，家用路由器、企业出口 |
- **PAT 原理**：将多个私有 IP 的流量映射到同一个公网 IP，通过端口号区分不同会话
- **优缺点**：节约 IP 地址，但破坏了端到端通信透明性（内网服务无法被外网直接访问）

**VLAN（虚拟局域网）**：
- **功能**：在物理网络上划分逻辑隔离的广播域
- **实现方式**：
  - 基于端口划分（Port-based）
  - 基于 MAC 地址划分
  - 基于协议划分（IP 子网）
  - 基于用户身份
- **VLAN 间路由**：通过三层交换机或路由器实现不同 VLAN 间的通信
- **Trunk 链路**：使用 802.1Q 标记，一条链路上传输多个 VLAN 的流量

**VPN（虚拟专用网络）**：
- **功能**：在公共网络上建立加密的私有通信隧道
- **VPN 协议对比**：

| 协议 | 层次 | 加密 | 端口 | 特点 |
|------|------|------|------|------|
| **IPsec** | 网络层 | 强 | 500/UDP（IKE） | 站点到站点，最常用 |
| **SSL/TLS VPN** | 传输层 | 强 | 443/TCP | 远程接入，无需客户端 |
| **PPTP** | 数据链路层 | 弱（MPPE） | 1723/TCP | 配置简单，安全性差 |
| **L2TP** | 数据链路层 | 无（需配合IPsec） | 1701/UDP | 与 IPsec 配合使用 |

**其他关键网络技术**：
- **ACL（访问控制列表）**：在路由器/交换机上设置流量过滤规则
- **QoS（服务质量）**：对不同类型的流量进行优先级调度，保障关键业务带宽
- **MPLS（多协议标签交换）**：通过标签实现快速转发，支持流量工程和 VPN

#### ***案例分析***

**【真题】**
为了实现内部网络与外部公网的通信，小型企业通常在出口路由器上配置（）技术。

**解答**：PAT（端口地址转换）。通过将内部私有 IP 映射到路由器外网接口的一个公网 IP，使用不同端口号区分内部会话。

**【真题】**
以下关于 VPN 的描述中，错误的是（）。
A. VPN 可以在公共网络上建立加密隧道
B. 使用 VPN 可以隐藏内部网络拓扑
C. VPN 可以保证数据传输的绝对安全
D. VPN 可以用于远程办公接入

**解答**：C。VPN 提供了加密传输和身份认证，但不能保证"绝对安全"。安全性还取决于加密强度、密钥管理、端点安全等因素。

---

## 6. 系统配置与性能评价

### ***背景***

系统性能评价是对计算机系统、网络、操作系统、数据库等组件的性能指标进行度量和分析的过程。

### ***基础原理推导***

**性能评价指标分类**：

| 系统类型 | 关键指标 |
|---------|---------|
| 计算机 | 主频、运算速度、内存容量、存取周期、吞吐率 |
| 路由器 | 设备吞吐量、端口吞吐量、丢包率、时延 |
| 交换机 | 背板吞吐量、缓冲区大小、MAC 地址表大小 |
| 网络 | 设备级、网络级、应用级、用户级性能指标 |
| 操作系统 | 可靠性、吞吐率、响应时间、资源利用率 |
| 数据库 | 最大连接数、最大并发事务、负载均衡能力 |
| Web 服务器 | 最大并发连接数、响应延迟、吞吐量 |

### ***公式总结***

**阿姆达尔解决方案（Amdahl's Law）**：
```
加速比 S = 1 / ((1 - f) + f/k)
```
其中 f 为可改进部分的比例，k 为改进后的加速倍数

**要点**：系统的性能提升受限于不可改进部分的占比。

### ***案例分析***

**【真题】**
对计算机评价的主要性能指标有时钟频率、（）、运算精度和内存容量等。

**答案**：D. 数据处理速率

---

## 7. 信息系统基础知识

### ***背景***

信息系统是由计算机硬件、网络和通信设备、计算机软件、信息资源、信息用户和规章制度组成的以处理信息流为目的的人机一体化系统。信息系统的 5 个基本功能：输入、存储、处理、输出和控制。

### ***基础原理推导***

**诺兰模型**：信息系统进化的 6 个阶段
1. **初始阶段**：计算机刚进入企业，仅用于财务等少数部门
2. **传播阶段**：各部门纷纷使用，但缺乏统一规划，效率低
3. **控制阶段**：从整体上控制发展，数据共享问题凸显
4. **集成阶段**：重新规划设计，建立统一信息管理系统
5. **数据管理阶段**：信息成为企业重要资源，统一平台
6. **成熟阶段**：信息系统满足各层次需求，IT 与管理融合

**信息系统分类（从低级到高级）**：
- **TPS/DPS（业务处理系统）**：局部事务管理
- **MIS（管理信息系统）**：企业全局信息管理
- **DSS（决策支持系统）**：支持半结构化决策
- **专家系统**：利用专家知识解决特定领域问题
- **OAS（办公自动化系统）**：办公事务自动化

### ***案例分析***

**【真题】**
诺兰模型将计算机信息系统的发展道路划分为 6 个阶段，其中（）阶段是计算机管理变为数据管理的关键。

**答案**：控制阶段（第 3 阶段）

---

## 8. 系统安全

### 8.1 信息安全基础

#### ***背景***

信息安全是指保护信息系统中的硬件、软件和数据，防止因偶然或恶意的原因而遭到破坏、更改或泄露。

#### ***基础原理推导***

**信息安全五要素**：
- **机密性**：信息不泄露给非授权用户
- **完整性**：信息在传输或存储过程中不被篡改
- **可用性**：授权用户可以正常访问信息
- **可控性**：对信息的传播及内容具有控制能力
- **不可否认性**：信息处理行为的不可抵赖性

**对称加密 vs 非对称加密**：

| 特性 | 对称加密 | 非对称加密 |
|------|---------|-----------|
| 密钥 | 加密解密密钥相同 | 公钥加密，私钥解密 |
| 速度 | 快 | 慢 |
| 安全性 | 密钥易泄露 | 安全性高 |
| 典型算法 | DES、3DES、AES、IDEA | RSA、Elgamal、ECC |
| 适用场景 | 加密大数据 | 加密对称密钥、数字签名 |

**数字信封**：对称加密数据 + 非对称加密对称密钥
**数字签名**：发送方用私钥签名，接收方用公钥验证
**信息摘要**：哈希函数生成固定长度摘要，用于完整性验证

### ***案例分析***

**【真题】**
完整的信息安全系统至少包含三类措施，即技术方面的安全措施、管理方面的安全措施和相应的（）。

**答案**：B. 政策法律

---

## 9. 软件工程

### 9.1 软件过程模型

#### ***背景***

软件过程模型是软件开发全部过程、活动和任务的结构框架。常见的模型包括瀑布模型、螺旋模型、迭代模型、敏捷开发等。

#### ***基础原理推导***

**瀑布模型（SDLC）**：
- 阶段：可行性分析→需求分析→设计→编码→测试→运行维护
- 特点：阶段顺序执行，文档驱动，适合需求明确的项目
- 缺点：难以应对需求变更，返工成本高

**螺旋模型**：
- 将原型迭代与瀑布模型结合
- 四个象限：制订计划→风险分析→实施工程→评审
- 适合大型、高风险项目

**敏捷开发**：
- 迭代增量开发，快速交付
- Scrum、XP（极限编程）、Kanban

#### ***案例分析***

**【真题】**
在软件开发模型中，（）特别适合于那些需求不稳定、开发风险较大的大型软件项目。

**解答**：螺旋模型。因为螺旋模型强调了风险分析，适合高风险项目。

---

### 9.2 软件能力成熟度模型（CMMI）

#### ***背景***

CMMI（能力成熟度模型集成）是软件工程、系统工程等多个学科领域的过程改进框架。

#### ***基础原理推导***

**阶段式模型（5 个成熟度级别）**：

| 级别 | 名称 | 特点 |
|------|------|------|
| 1 | 初始级 | 过程不可预测，缺乏控制 |
| 2 | 已管理级 | 过程为项目服务 |
| 3 | 已定义级 | 过程为组织服务 |
| 4 | 定量管理级 | 过程已度量和控制 |
| 5 | 优化级 | 持续过程改进 |

**连续式模型**：关注每个过程域的能力，一个组织对不同的过程域可以达到不同的能力等级。

---

## 10. 面向对象技术

### 10.1 面向对象开发

#### ***背景***

面向对象技术是一种以对象为核心的软件开发方法，强调数据和操作数据的函数的封装。

#### ***基础原理推导***

**面向对象的分析（OOA）**：五个活动
1. 认定对象
2. 组织对象
3. 描述对象间的相互作用
4. 确定对象的操作
5. 定义对象的内部信息

**面向对象的设计原则（SOLID）**：
- **单一责任原则（SRP）**：一个类只做一件事
- **开放-封闭原则（OCP）**：对扩展开放，对修改关闭
- **里氏替换原则（LSP）**：子类必须能替换父类
- **接口分离原则（ISP）**：不强迫客户端依赖不需要的方法
- **依赖倒置原则（DIP）**：依赖抽象，不依赖具体实现

#### ***案例分析***

**【真题】**
面向对象的分析模型主要由顶层架构图、用例与用例图和（）构成；设计模型则包含以（）表示的软件体系结构图。

**解答**：领域概念模型；包图

---

## 11. 项目管理

### ***背景***

项目管理是将知识、技能、工具和技术应用于项目活动，以满足项目要求。

### ***公式总结***

**关键路径法（CPM）**：
- 关键路径是项目中时间最长的路径，决定了项目的最短工期
- 松弛时间（浮动时间）= 最晚开始时间 - 最早开始时间

**挣值管理（EVM）**关键指标：
| 指标 | 公式 | 含义 |
|------|------|------|
| PV（计划价值） | 计划完成的工作量价值 | - |
| EV（挣值） | 实际完成的工作量价值 | - |
| AC（实际成本） | 实际花费的成本 | - |
| SV（进度偏差） | EV - PV | >0 表示进度超前 |
| CV（成本偏差） | EV - AC | >0 表示成本节约 |
| SPI | EV / PV | >1 表示进度超前 |
| CPI | EV / AC | >1 表示成本节约 |

### ***案例分析***

**【真题】**
某项目计划工期 10 个月，第 5 个月末时，计划应完成工作量的预算成本（PV）为 100 万元，实际完成工作量的预算成本（EV）为 80 万元，实际花费（AC）为 90 万元。求 SV、CV、SPI、CPI。

**解答**：
SV = 80 - 100 = -20 万元（进度滞后）
CV = 80 - 90 = -10 万元（成本超支）
SPI = 80/100 = 0.8（进度落后计划）
CPI = 80/90 ≈ 0.89（成本超支）

---

## 12. 系统架构设计

### 12.1 软件架构概述

#### ***背景***

软件架构是需求分析到软件设计之间的过渡过程。只要软件架构设计好了，整个软件就不会出现系统性的错误。架构设计就是需求分配，将满足需求的职责分配到组件上。

**软件架构的作用**：
- 项目干系人进行交流的手段
- 明确对系统实现的约束条件
- 决定开发和维护的组织结构
- 制约系统的质量属性
- 可传递和可复用的模型

#### ***基础原理推导***

**软件架构设计与生命周期**：
1. **需求分析阶段**：从问题空间向解空间转换
2. **设计阶段**：架构模型的描述、分析与设计方法
3. **实现阶段**：从架构向实现过渡，构件组装
4. **构件组装阶段**：可复用构件的互联，解决架构失配
5. **部署阶段**：软硬件部署模型分析
6. **后开发阶段**：动态架构、架构恢复与重建

**构件（Component）**：
- 独立可交付的功能单元，通过接口提供服务
- 特性：独立部署单元、第三方组装单元、无外部可见状态
- 构件标准：EJB（Java）、COM/DCOM/COM+（微软）、CORBA（OMG）

### 12.2 软件架构风格

#### ***背景***

软件体系结构风格是描述某一特定应用领域中系统组织方式的惯用模式。架构风格定义一个系统家族，即一个词汇表和一组约束。

#### ***基础原理推导***

**五大架构风格**：

| 风格 | 特点 | 代表 |
|------|------|------|
| **数据流风格** | 数据按顺序从前向后处理 | 批处理、管道-过滤器 |
| **调用/返回风格** | 构件间显式调用 | 主程序/子程序、面向对象、层次结构 |
| **独立构件风格** | 构件间通过事件触发异步执行 | 进程通信、事件驱动（隐式调用） |
| **虚拟机风格** | 自定义规则集，跨平台适配 | 解释器、基于规则的系统 |
| **仓库风格** | 以数据为中心，围绕共享数据 | 数据库系统、黑板系统 |

**管道-过滤器风格特点**：
- 过滤器独立，不共享状态
- 可并发执行
- 可任意组合和替换
- 通过数据流隐式交互
- 缺点：不适合交互性强的应用

#### ***案例分析***

**【真题】**
某日志分析系统需要按"采集-过滤-分析-输出"的流程处理数据，各步骤独立运行且可替换。该系统最适合采用的架构风格是？

**解答**：B. 管道-过滤器风格

**【IDE 架构对比真题】**：
- 管道-过滤器：适合编译，不适合交互式调试
- 仓库风格（黑板）：适合 IDE，支持断点调试和多工具协同

#### ***难题解析***

**【概念辨析题】**
以下关于管道-过滤器架构风格的描述中，错误的是：
A. 体现了各功能模块高内聚、低耦合的"黑盒"特性
B. 支持软件功能模块的重用，便于系统维护
C. 适合交互性强的应用，因为数据可以边生产边消费
D. 整体延迟等于所有过滤器处理时间之和

**解析**：C 错误。管道过滤器本质是批处理风格，不适合交互性强的应用（用户无法在中间步骤干预）。

**正确答案：C**

---

### 12.3 架构评估方法（ATAM/SAAM）

#### ***背景***

软件架构评估方法起源于 20 世纪 90 年代，由卡内基梅隆大学软件工程研究所（CMU/SEI）提出。目的是在系统开发早期识别架构风险，避免后期返工成本。

#### ***基础原理推导***

**SAAM（Software Architecture Analysis Method）**：1990 年代初提出，核心目标是评估架构的可修改性。

**ATAM（Architecture Tradeoff Analysis Method）**：2000 年提出（SAAM 升级版），核心目标是全面评估多个质量属性之间的权衡。

**核心概念**：
- **敏感点**：某个架构参数的变化显著影响某个质量属性
- **权衡点**：某个架构参数对多个质量属性有相反影响
- **风险点**：架构决策可能导致某个质量属性无法满足需求

#### ***公式总结***

| 方法 | SAAM | ATAM |
|------|------|------|
| 提出时间 | 1990s 初 | 2000 年 |
| 核心目标 | 可修改性评估 | 多质量属性权衡分析 |
| 核心工具 | 场景 | 效用树 + 场景 |
| 阶段数 | 5 个 | 9 个 |

**ATAM 9 个阶段**：方法介绍→商业动机→架构介绍→识别架构方法→生成质量属性效用树→分析架构方法→头脑风暴和优先级排序→分析架构方法（第二轮）→结果呈现

#### ***案例分析***

**【典型权衡点案例】**

| 架构决策 | 提高的属性 | 降低的属性 |
|---------|-----------|-----------|
| 数据冗余备份 | 可用性 | 性能、成本 |
| 引入缓存层 | 性能 | 一致性、可维护性 |
| 微服务拆分 | 可修改性 | 性能、复杂度 |
| 数据加密传输 | 安全性 | 性能 |

#### ***难题解析***

**【概念辨析题】**
以下关于架构评估中敏感点和权衡点的描述，正确的是：
A. 敏感点是影响多个质量属性的架构参数
B. 权衡点是只影响一个质量属性的架构参数
C. 敏感点和权衡点都是关键的架构参数，区别在于影响的质量属性数量
D. 风险点就是权衡点的另一种说法

**解析**：A 错（敏感点影响一个属性），B 错（权衡点影响多个属性），D 错（风险点是"可能出问题的地方"）

**正确答案：C**

---

## 13. 软件可靠性基础

### 13.1 可靠性指标与串并联计算

#### ***背景***

可靠性工程起源于 20 世纪 50 年代的美国军事领域。软件可靠性是可靠性工程在软件领域的延伸，与硬件可靠性不同，软件故障主要源于设计缺陷而非物理磨损。

#### ***基础原理推导***

**可靠性的概率本质**：
可靠性 R(t) 是系统在时间 [0,t] 内不发生故障的概率。

**指数分布模型**：
假设失效率 λ 为常数，则：
- 可靠度函数：R(t) = e^(-λt)
- 失效率 λ：单位时间故障概率

**软件可靠性和硬件可靠性区别**：
| 软件 | 硬件 |
|------|------|
| 不存在物理退化 | 物理退化导致失效 |
| 所有 COPY 版本相同 | 两个硬件不可能完全一样 |
| 版本更新周期快 | 版本更新周期慢 |
| 复杂性高 | 复杂性相对低 |

**提高系统可靠性的技术**：
- **避错（排错）**：技术评审、系统测试、正确性证明
- **容错**：冗余方法（结构冗余、信息冗余、时间冗余、冗余附加）

**软件容错主要技术**：
- **N 版本程序设计**：多个独立团队开发同一功能的 N 个版本，运行表决
- **恢复块方法**：主块执行失败时切换到备份块
- **防卫式程序设计**：在程序中加入异常处理和保护代码

#### ***公式总结***

| 指标 | 符号 | 公式 | 含义 |
|------|------|------|------|
| 失效率 | λ | 单位时间故障概率 | 次/小时 |
| 平均无故障时间 | MTTF | 1/λ | 故障前平均工作时间 |
| 平均故障间隔时间 | MTBF | 1/λ | 两次故障间隔时间 |
| 平均修复时间 | MTTR | - | 故障修复平均耗时 |
| 可用性 | A | MTBF/(MTBF+MTTR) | 正常运行时间比例 |

**系统可靠性计算**：

| 系统结构 | 可靠度公式 | 特点 |
|---------|-----------|------|
| 串联 | R = R₁×R₂×...×Rₙ | 低于任何部件 |
| 并联 | R = 1-(1-R₁)(1-R₂)...(1-Rₙ) | 高于任何部件 |
| 双部件并联 | R = 2R - R² | 常用冗余配置 |
| 三模冗余（TMR） | R = 3R² - 2R³ | 2/3 表决 |

#### ***案例分析***

**【2023 年真题】**
某系统由 3 个部件串联组成，每个部件的可靠度均为 0.9。求系统整体可靠度。

**解答**：
R = 0.9 × 0.9 × 0.9 = 0.729

**【2022 年真题】**
某系统采用双机热备方案，单机可靠度为 0.9。求系统可靠度。

**解答**：
R = 1 - (1-0.9)² = 1 - 0.01 = 0.99

**【可用性计算真题】**
某服务器平均每 1000 小时发生一次故障，每次故障平均需要 2 小时修复。求该服务器的可用性。

**解答**：
MTBF = 1000 小时，MTTR = 2 小时
A = 1000 / (1000 + 2) ≈ 0.998 = 99.8%

#### ***难题解析***

**【拔高题·三模冗余阈值】**
某计算机系统采用三模冗余（TMR）设计，每个模块的可靠度为 R。当 R 取何值时，三模冗余的可靠度高于单机？

**解析**：
三模冗余可靠度：R_TMR = 3R² - 2R³
单机可靠度：R_single = R

令 R_TMR > R_single：
3R² - 2R³ > R
3R - 2R² > 1（两边除以 R，R>0）
2R² - 3R + 1 < 0
(2R - 1)(R - 1) < 0

解这个不等式：当 0.5 < R < 1 时，不等式成立

**结论**：当单机可靠度大于 0.5 时，三模冗余能提高可靠性；如果单机可靠度低于 0.5，表决器反而会"多数服从错误的多数"，可靠性更低。

---

## 14. 软件架构的演化和维护

### ***背景***

软件架构的演化和维护是对架构进行修改和完善的过程，目的是使软件能够适应环境的变化。

### ***基础原理推导***

**演化重要性**：
- 架构是整个系统的骨架
- 架构作为系统蓝图为管控复杂性和变化性提供途径

**面向对象软件架构演化**：
- **对象演化**：AO（添加对象）、DO（删除对象）
- **消息演化**：AM/DM/SMO/OM/CMM
- **复合片段演化**：AF/DF/FTC/FCC
- **约束演化**：AC/DC

**静态演化步骤**：
软件理解 → 需求变更分析 → 演化计划 → 系统重构 → 系统测试

**大型网站架构演化典型路径**：
单一应用 → 应用+数据库分离 → 引入缓存/CDN → 分布式数据库 → 微服务化

---

## 15. 未来信息综合技术

### 15.1 信息物理系统（CPS）

#### ***背景***

CPS 是控制系统、嵌入式系统的扩展与延伸，通过集成先进的感知、计算、通信、控制等信息技术，构建物理空间与信息空间中人、机、物、环境等要素相互映射的系统。

#### ***基础原理推导***

**CPS 体系架构**：
1. **单元级 CPS**：最小不可分割单元，具备可感知、可计算、可交互功能
2. **系统级 CPS**：多个单元级通过工业网络互联互通
3. **SoS 级 CPS**：多个系统级组合，实现数据汇聚和全局优化

**CPS 四大核心技术要素**：
- 一硬：感知和自动控制
- 一软：工业软件
- 一网：工业网络
- 一平台：工业云和智能服务平台

### 15.2 人工智能

**关键技术**：
- 机器学习（监督学习、无监督学习、强化学习）
- 深度学习（CNN、RNN、Transformer）
- 自然语言处理（NLP）
- 计算机视觉
- 专家系统

### 15.3 边缘计算

**概念**：将计算和数据存储推向网络边缘，靠近数据源进行处理，减少延迟和带宽消耗。

### 15.4 数字孪生

**概念**：物理实体的数字化镜像，通过实时数据同步实现仿真、预测和优化。

### 15.5 云计算和大数据

**云计算服务模式**：
- IaaS（基础设施即服务）：提供计算资源
- PaaS（平台即服务）：提供开发运行平台
- SaaS（软件即服务）：提供应用软件

---

## 16. 补充-数学与经济管理

### 16.1 最小生成树

**算法**：
- **Prim 算法**：从一个顶点出发，每次选择最短边连接新顶点
- **Kruskal 算法**：按权重从小到大选择边，不形成环

### 16.2 最短路径

**算法**：
- **Dijkstra 算法**：单源最短路径，适合非负权图
- **Floyd 算法**：多源最短路径，适合任意权图

### 16.3 网络与最大流量

**Ford-Fulkerson 算法**：寻找增广路径直到无法找到

### 16.4 线性规划

**核心**：在约束条件下求目标函数的最大值或最小值。图解法适合二维，单纯形法适合高维。

### 16.5 动态规划

**核心思想**：将复杂问题分解为重叠子问题，记录子问题的解避免重复计算。

**经典问题**：背包问题、最长公共子序列、最短路径

### 16.6 博弈论

**核心概念**：
- 零和博弈：一方所得等于另一方所失
- 纳什均衡：各方都不能通过单方面改变策略而获益

### 16.7 决策论

**决策矩阵**：方案 × 状态，每个格子是收益值
- 乐观准则（最大最大）：取每个方案最大收益中的最大值
- 悲观准则（最大最小）：取每个方案最小收益中的最大值
- 最小后悔值准则：构建后悔值矩阵，取最小化最大后悔值

### 16.8 数学建模

**步骤**：问题分析 → 模型假设 → 模型建立 → 模型求解 → 模型检验 → 模型应用

---

## 17. 补充-知识产权与标准化

### 17.1 知识产权概述

**知识产权**是指公民、法人、非法人单位对自己的创造性智力成果依法享有的民事权。

**四个特性**：
- **无体性**：无具体形体，是智力创造成果
- **专有性**：未经权利人同意，他人不得使用
- **地域性**：只在权利授予国受保护
- **时间性**：只在法律规定期限内受保护

### 17.2 保护期限

| 客体类型 | 权力类型 | 保护期限 |
|---------|---------|---------|
| 公民作品 | 署名权、修改权、保护作品完整权 | 无限期 |
| 公民作品 | 发表权、使用权 | 作者终生+死后50年 |
| 单位作品 | 发表权、使用权 | 首次发表后50年 |
| 公民软件 | 发表权、复制权等 | 作者终生+死后50年 |
| 单位软件 | 发表权、复制权等 | 首次发表后50年 |
| 注册商标 | 商标权 | 10年（可续展） |
| 发明专利 | 专利权 | 20年（从申请日算） |
| 实用新型 | 专利权 | 10年（从申请日算） |

### 17.3 知识产权人的确定

**职务作品**：
- 利用单位资源创作并由单位承担责任 → 著作权归单位（署名权除外）
- 有合同约定归单位 → 著作权归单位
- 其他 → 作者拥有著作权，单位优先使用

**职务软件**：
- 属于本职工作中明确规定的开发目标 → 单位享有著作权
- 属于从事本职工作活动的结果 → 单位享有著作权

### 17.4 侵权判断

**侵权判定要点**：
- 未经许可使用他人作品 → 侵权
- 适当引用（注明出处）→ 合理使用，不侵权
- 为个人学习、研究使用 → 合理使用
- 国家机关为执行公务使用 → 合理使用

---

> **内容说明**：本复习材料综合了考纲要求、历年真题、参考教材和在线资源整理而成。每个知识点均按照 ***背景*** → ***基础原理推导*** → ***公式总结*** → ***案例分析*** → ***难题解析*** 的五段式结构组织。建议结合历年真题进行强化训练。
