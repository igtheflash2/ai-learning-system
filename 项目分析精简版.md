# AI学习资料生成工具 — 项目分析

> 路径: `d:\杂件\机器人\readpdf\`  
> 功能: 将教材 PDF/Word/Excel 经 OCR + 本地清洗 + LLM 增强, 输出结构化学习资料 + 思维导图 + Word + PNG

---

## 一、目录结构

```
readpdf/
├── app.py                    # Streamlit 入口（旧版）
├── readpdf.py                # CLI 主入口
├── config.json               # 用户配置（API Key/模型等）
├── modules/                  # 核心模块
│   ├── config_manager.py     #   配置 + 水印清洗 + 章节顺序
│   ├── ocr_engine.py         #   PDF OCR → Markdown
│   ├── local_processor.py    #   本地清洗/结构化
│   ├── ai_generator.py       #   LLM 调用（学习资料+思维导图）
│   ├── prompt_manager.py     #   Jinja2 模板渲染
│   ├── docx_converter.py     #   Markdown → Word
│   └── mindmap_renderer.py   #   Mermaid → PNG
├── backend/                  # Flask 后端 + HTML 前端
│   ├── app.py                #   API 服务器（SSE 流式）
│   ├── app_state.py          #   全局状态
│   └── static/               #   前端资源
│       ├── index.html        #   交互工作台
│       ├── css/style.css     #   暗色主题
│       └── js/app.js         #   前端逻辑
├── prompts/                  # LLM 提示词模板
│   ├── study_guide.j2
│   └── mindmap.j2
├── input/                    # 上传文件
├── output_md/                # OCR 结果（按章节）
├── output_word/
├── output_excel/
├── temp_output/
├── readword.py               # Word 提取
└── readexcel.py              # Excel 提取
```

---

## 二、模块说明

| 模块 | 职责 |
|------|------|
| `config_manager.py` | `AppConfig` 加载配置; `CHAPTER_ORDER` (19章); `WATERMARK_PATTERNS` (30+水印正则); `clean_line()` |
| `ocr_engine.py` | rapidocr-onnxruntime + PyMuPDF + OpenCV, 扫描PDF→Markdown, 支持批量+回调 |
| `local_processor.py` | `process_local_study_guide()` 读取章节MD, 去水印清洗, 按顺序拼接 |
| `ai_generator.py` | 封装OpenAI SDK, 兼容DeepSeek; `generate_study_guide()` (完整/逐章); `generate_mindmap()`; 流式callback |
| `prompt_manager.py` | Jinja2渲染 `.j2` 模板, 无依赖时降级为 `{{ var }}` 替换 |
| `docx_converter.py` | python-docx, 支持多级标题/表格/代码块/列表, 微软雅黑 |
| `mindmap_renderer.py` | mmdc (Node.js) 或 Playwright 两种方案, 自动回退 |

---

## 三、5 步流水线

```
Step 1 本地清洗    process_local_study_guide() → 去水印+排序拼接
Step 2 AI 学习资料  generate_study_guide() → LLM 流式输出, 保存 MD
Step 3 AI 思维导图  generate_mindmap() → Mermaid + Markdown
Step 4 Word 导出   convert_markdown_to_docx() → .docx
Step 5 PNG 渲染    render_mindmap_to_png() → .png (mmdc/playwright)
```

---

## 四、后端 API

| 路由 | 功能 |
|------|------|
| `GET /api/config` / `POST /api/config` | 配置读写 |
| `POST /api/config/verify` | 验证API连通性 |
| `GET /api/chapters` | 可用章节列表 |
| `GET /api/upload/pdf` (SSE) | 上传PDF+实时OCR进度 |
| `POST /api/upload/word` / `excel` | 上传文档 |
| `POST /api/pipeline/start` | 启动全流程 |
| `GET /api/pipeline/stream/<id>` (SSE) | 实时进度 |
| `GET /api/download/*` | 下载结果 |
| `GET /api/files/md` / `md/<path>` | 知识库浏览 |
| `GET/POST /api/prompts/<name>` | 模板编辑 |

---

## 五、配置项 (`config.json`)

`run_mode`, `api_key`, `base_url`, `model`, `max_tokens`, `generation_mode`, `focus_areas`, `detail_level`, `include_mnemonics`, `include_exercises`

---

## 六、启动

```bash
# 新版（推荐）
python backend/app.py  →  http://localhost:5001

# 旧版
streamlit run app.py

# CLI
python readpdf.py
```

---

## 七、扩展点

| 需求 | 位置 |
|------|------|
| 新 OCR 引擎 | `modules/ocr_engine.py` |
| 新章节/教材 | `modules/config_manager.py` 的 `CHAPTER_ORDER` |
| 新水印规则 | `modules/config_manager.py` 的 `WATERMARK_PATTERNS` |
| 修改 prompt | `prompts/*.j2` 或提示词工作台在线编辑 |
| 新 API 路由 | `backend/app.py` 添加 `@app.route` |
| 新输入格式 | 参考 `readword.py` / `readexcel.py` 模式 |
